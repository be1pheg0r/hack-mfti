shouldSplitInstructionPrompt = """
Ты анализируешь объявления об услугах ремонта.
Твоя задача: определить, нужно ли выделять дополнительные микрокатегории в отдельные объявления.

Микрокатегории:
102: Сантехника — трубы, унитазы, смесители, водоснабжение
103: Электрика — проводка, розетки, щитки, светильники
104: Натяжные потолки
105: Укладка плитки — кафель, керамогранит, мозаика
106: Поклейка обоев
107: Малярные работы — покраска стен, потолков
108: Штукатурные работы — штукатурка, шпаклёвка
109: Напольные покрытия — ламинат, линолеум, паркет
110: Гипсокартон
111: Демонтажные работы

Ключевое правило:
- буллеты, нумерация и перечисления сами по себе нейтральны: это не автоматический False и не автоматический True.
- оценивай смысл: есть ли в тексте отдельные виды работ, которые можно выделить в самостоятельные микрокатегории.

Примеры, где обязательно shouldSplit=False:
1) "выполняю ремонтные и отделочные работы от эконом класса до люкс: ...\n- шпатлевка и штукатурка стен;\n- демонтаж стен и перегородок;\n- укладка кафельной плитки и напольных покрытий;\n- установка сантехники; ..."
   Причина: автор просто перечисляет, что делает в рамках одного объявления.
2) "Стрoительствo и Ремонт ... НАШИ УСЛУГИ👇 ... ✅ ... ✅ ..."
   Причина: длинный список услуг и преимущества бригады, но это единое объявление-описание, не сигнал к split.
3) "укладка плитки в доме квартире быстро качественно и не дорого"
   Причина: в запросе только одна микрокатегория (укладка плитки), shouldSplit=False.

True если:
- в тексте есть хотя бы одна дополнительная самостоятельная микрокатегория, которую можно выделить отдельно;
- даже если все написано в одном объявлении и в виде списка;
- даже если выделяется только одна дополнительная микрокатегория (например только сантехника).

Пример, где обязательно shouldSplit=True:
- "Устанавливаем натяжные потолки любой сложности ... Цена за кв.м."
  Причина: самостоятельная отдельная услуга (одна микрокатегория), поданная как отдельный оффер.
- "Предлагаем профессиональную услугу по малярным работам ... Шпатлевка стен под обои/покраску, окраска стен, оклейка обоями"
  Причина: отдельный специализированный оффер по малярным работам с узким набором близких операций.
- "Качественные ремонты ванных комнат и сан.узлов под ключ ... плитка (керамогранит, мозаика), установка смесителей/раковин/унитазов/душевых, водоснабжение, канализация, услуги электрика"
  Причина: перечислены самостоятельные услуги из разных микрокатегорий (105 + 102 + 103), значит shouldSplit=True даже без явных слов-маркеров.

False если:
- в тексте не удается выделить ни одной дополнительной микрокатегории;
- описание полностью про единый комплекс без самостоятельных направлений работ;
- есть только общие фразы без явных признаков дополнительных категорий.

Похожие объявления с ответами помогут тебе откалиброваться.
Верни строго JSON-объект без пояснений:
{"shouldSplit": true/false}
"""


def shouldSplitPromptWithRAG(desc: str, top_k: list) -> str:
    top_k_text = "\n".join(
        f"{i + 1}. Вердикт: {'True' if shouldSplit else 'False'} | Категории: {', '.join(cats) if cats else 'нет'}\n   Текст: {text[:300]}"
        for i, (text, shouldSplit, cats) in enumerate(top_k)
    )

    return f"""
    Ниже будут объявления из векторной БД.
    ОСНОВЫВАЙ решение на их вердиктах и категориях, не делай выводы из домыслов.

    Похожие объявления из базы:
{top_k_text}

Объявление для классификации:
{desc}

Важно: буллеты/нумерация и формулировки вроде "отдельно" сами по себе нейтральны.
Принимай решение по смыслу текста.
Ставь shouldSplit=True, если можно выделить хотя бы одну дополнительную самостоятельную микрокатегорию.

Верни JSON c полем shouldSplit."""


def shouldSplitPromptWithoutRAG(desc: str) -> str:
    """Формирует промпт should-split без retrieval-примеров."""
    return f"""Объявление для классификации:
{desc}

Важно: буллеты/нумерация и формулировки вроде "отдельно" сами по себе нейтральны.
Принимай решение по смыслу текста.
Ставь shouldSplit=True, если можно выделить хотя бы одну дополнительную самостоятельную микрокатегорию.

Верни JSON c полем shouldSplit."""


categorizationInstructionPrompt = """
Ты определяешь микрокатегории услуг в объявлении о ремонте.

Доступные микрокатегории:
102: Сантехника — трубы, унитазы, смесители, водоснабжение, канализация
103: Электрика — проводка, розетки, щитки, светильники, монтаж проводки
104: Натяжные потолки — монтаж натяжных потолков
105: Укладка плитки — кафель, керамогранит, мозаика, плиточные работы
106: Поклейка обоев — поклейка, снятие обоев
107: Малярные работы — покраска стен, потолков
108: Штукатурные работы — штукатурка, шпаклёвка, выравнивание стен
109: Напольные покрытия — ламинат, линолеум, паркет, кварцвинил, стяжка
110: Гипсокартон — монтаж гипсокартона, перегородки из гкл
111: Демонтажные работы — демонтаж, снос перегородок, вывоз мусора

Верни ТОЛЬКО список id микрокатегорий через запятую, которые явно присутствуют в тексте.
Не включай категории которые лишь косвенно упомянуты.
Список может содержать одну категорию, если выделяется только одно дополнительное направление.
Пример: "Ремонт квартир под ключ. Частичный ремонт квартир. Ремонт ванной комнаты. Отдельные виды работ." -> 102
Пример ответа: 102, 103, 108
"""

def categorizationPromptWithRAG(desc: str, top_k: list) -> str:
    top_k_text = "\n".join(
        f"{i + 1}. Категории: {', '.join(str(c) for c in cats) if cats else 'нет'}\n   Текст: {text[:300]}"
        for i, (text, _, cats) in enumerate(top_k)
        if _  # только True примеры
    )

    return f"""Похожие объявления где выделялись категории:
{top_k_text}

Объявление для анализа:
{desc}

Основывайся на категориях из RAG-примеров и тексте объявления, не добавляй категории из предположений.

Какие микрокатегории присутствуют? Верни только id через запятую (может быть одна категория)."""


def categorizationPromptWithoutRAG(desc: str) -> str:
    """Формирует промпт категоризации без retrieval-примеров."""
    return f"""Объявление для анализа:
{desc}

Какие микрокатегории присутствуют? Верни только id через запятую (может быть одна категория)."""



draftGenerationInstructionPrompt = """
Ты пишешь черновик объявления услуги.

Требования:
- текст должен быть кратким и информативным;
- текст должен звучать в том же стиле, что и исходное объявление (лексика, тон, подача), но орфографические ошибки можно исправить;
- тон должен быть сдержанным и нейтрально-деловым, без агрессивной рекламы;
- не добавляй вымышленных фактов, обещаний, гарантий, скидок, сроков и других условий, которых нет в исходном тексте;
- НИ ПРИ КАКИХ УСЛОВИЯХ не изменяй, не округляй, не пересчитывай и не подставляй новые цены;
- если цена есть во входном тексте, можно использовать только ее исходное значение без изменений;
- не добавляй имя исполнителя, контакты, "звоните", эмодзи, капслок и markdown-выделение (**...**), если этого нет в исходном тексте;
- фокус только на указанной микрокатегории;
- верни только готовый текст черновика без пояснений.
"""


def draftGenerationPrompt(description: str, mc_title: str) -> str:
    """Формирует промпт генерации черновика по одной микрокатегории."""
    return f"""Исходное объявление:
{description}

Целевая микрокатегория: {mc_title}

Сформируй короткий информативный черновик в стиле исходного текста."""

